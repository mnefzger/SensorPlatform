<?xml version="1.0" encoding="utf-8"?>
<resources>

    <integer name="accelerometer_delay_default">60000</integer>

    <integer name="rotation_delay_default">60000</integer>

    <integer name="rawData_delay_default">500</integer>

    <integer name="rawData_delay_default">500</integer>

    <!-- Threshold is the g-force value
         e.g. 0.4 * 9.81 = 3.924
         Value of 0.4g based on: DriveSafe (Bergasa, 2014) -->
    <item name="acceleration_threshold_default" format="float" type="integer">3.924</item>

    <!-- Threshold in rad/s
         Value based on (Wang, 2013) -->
    <item name="rotation_threshold_normal_default" format="float" type="integer">0.3</item>

    <item name="rotation_threshold_risky_default" format="float" type="integer">0.5</item>

</resources>
